import { css } from 'lit';

export default css`
  :host {
    min-height: 100vh;
    display: flex;
    width: 100%;
  }

  .expand-panels :host {
    background-color: var(--slate-900);
  }

  .expand-panels :host {
    background-color: var(--slate-900);
  }
`;
