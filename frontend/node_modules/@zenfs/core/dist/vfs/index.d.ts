export { BigIntStatsFs, Stats, StatsFs } from '@zenfs/core/stats.js';
export * from '@zenfs/core/vfs/async.js';
export * as constants from '@zenfs/core/vfs/constants.js';
export * from '@zenfs/core/vfs/dir.js';
export * as promises from '@zenfs/core/vfs/promises.js';
export { chroot, mount, mountObject, 
/**
 * The map of mount points.
 * Using `fs.mounts` instead of the `mounts` export is a security issue and not recommended.
 * The deprecation here propagates to the correct `mounts` export for some reason.
 * @todo [BREAKING] remove `fs.mounts` for security.
 * @deprecated Use the `mounts` export that isn't an `fs` property!
 */
mounts, umount, } from '@zenfs/core/vfs/shared.js';
export * from '@zenfs/core/vfs/streams.js';
export * from '@zenfs/core/vfs/sync.js';
