import { StoreFS } from '@zenfs/core/backends/store/fs.js';
import { SimpleTransaction, type SimpleSyncStore } from '@zenfs/core/backends/store/simple.js';
/**
 * A simple in-memory store
 */
export declare class InMemoryStore extends Map<bigint, Uint8Array> implements SimpleSyncStore {
    name: string;
    constructor(name?: string);
    sync(): Promise<void>;
    clearSync(): void;
    transaction(): SimpleTransaction;
}
declare const _InMemory: {
    readonly name: "InMemory";
    readonly options: {
        readonly name: {
            readonly type: "string";
            readonly required: false;
        };
    };
    readonly create: ({ name }: {
        name?: string;
    }) => StoreFS<InMemoryStore>;
};
type _InMemory = typeof _InMemory;
export interface InMemory extends _InMemory {
}
/**
 * A simple in-memory file system backed by an InMemoryStore.
 * Files are not persisted across page loads.
 */
export declare const InMemory: InMemory;
export {};
